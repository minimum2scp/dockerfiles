#! /bin/sh

set -e
set -x
export DEBIAN_FRONTEND=noninteractive

for v in $(bash -lc "rbenv versions --bare --skip-aliases | grep -vE '^2\.[01].'"); do
  bash -lc "RBENV_VERSION=${v} rails new /tmp/build/rails5/hello-${v}"
done

