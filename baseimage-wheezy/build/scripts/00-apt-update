#! /bin/sh

set -e
set -x
export DEBIAN_FRONTEND=noninteractive

install -m 644 -o root -g root -p /build/etc/apt/sources.list /etc/apt/sources.list

apt-get update -o APT::Acquire::Retries=5
apt-get dist-upgrade -y --fix-missing --fix-broken

