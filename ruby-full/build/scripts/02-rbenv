#! /bin/sh

set -e
set -x
export DEBIAN_FRONTEND=noninteractive

## run rbenv-update
bash -l -c "rbenv update"

## install subversion to build ruby 1.8.7-p375 (requires svn commands)
#apt-get install --no-install-recommends -y subversion

## install pre-build binary (see https://github.com/minimum2scp/ruby-binary)
mkdir /tmp/build/ruby-full/tmp/ruby-binary
curl -L -o /tmp/build/ruby-full/tmp/ruby-binary/install.sh https://raw.githubusercontent.com/minimum2scp/ruby-binary/master/install.sh
chmod +x /tmp/build/ruby-full/tmp/ruby-binary/install.sh
/tmp/build/ruby-full/tmp/ruby-binary/install.sh v0.1.22

